// Routes es el tipo que representa el arreglo de rutas de Angular.
import { Routes } from '@angular/router';

// En este ejemplo no vamos a navegar entre varias páginas.
// Aun así se deja el archivo para mostrar la estructura correcta.
export const routes: Routes = [];